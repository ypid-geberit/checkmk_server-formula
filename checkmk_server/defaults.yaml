# -*- coding: utf-8 -*-
# vim: ft=yaml
---
checkmk_server:
  pkg:
    name: checkmk_server
  rootgroup: root
  config: '/etc/checkmk_server'
  service:
    name: checkmk_server
  subcomponent:
    config: '/etc/checkmk_server-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
